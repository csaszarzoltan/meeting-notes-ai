import { useEffect, useMemo, useState } from 'react';
import { workspaceRequest } from '../api/workspace';
interface WorkAction { id:string; title:string; owner:string; due:string; meeting:string; timestamp:string; status:string; destination:string; external_id?:string|null; }
const FILTERS=['Assigned to me','Unassigned','Due soon','Overdue','Waiting for approval','Queued','Completed'];
/** Persisted action confirmation and connector synchronization. */
export function ActionCenter(){const[items,setItems]=useState<WorkAction[]>([]);const[filter,setFilter]=useState('Assigned to me');const[error,setError]=useState('');const load=()=>workspaceRequest<{items:WorkAction[]}>('/actions').then(b=>setItems(b.items)).catch((e:Error)=>setError(e.message));useEffect(()=>{void load()},[]);const visible=useMemo(()=>items.filter(i=>filter==='Unassigned'?i.owner==='Unassigned':filter==='Overdue'?i.due==='Overdue':filter==='Queued'?i.status==='queued':filter==='Completed'?i.status==='completed':filter==='Waiting for approval'?i.status==='suggested':filter==='Assigned to me'?i.owner==='Zoltan':true),[items,filter]);const confirm=async(i:WorkAction)=>{await workspaceRequest(`/actions/${i.id}`,{method:'PATCH',body:JSON.stringify({status:'confirmed',owner:i.owner,due:i.due})});await load()};const sync=async(i:WorkAction)=>{await workspaceRequest(`/actions/${i.id}/queue`,{method:'POST',body:JSON.stringify({destination:i.destination})});await load()};return <section><div className="page-heading"><div><span className="eyebrow">Execution layer</span><h2>Action Center</h2><p>Confirm ownership and synchronize accountable work.</p></div></div>{error&&<p role="alert" className="error-banner">{error}</p>}<div className="filter-strip">{FILTERS.map(f=><button className={f===filter?'active':''} onClick={()=>setFilter(f)} key={f}>{f}</button>)}</div><div className="execution-list">{visible.map(i=><article className="execution-card" key={i.id}><div className="execution-main"><span className={`status-pill task-${i.status}`}>{i.status}</span><strong>{i.title}</strong><p>{i.owner} · {i.due} · {i.meeting}</p><button className="evidence-link">Source evidence · {i.timestamp}</button></div><div className="execution-destination"><small>{i.destination}</small>{i.status==='suggested'?<button className="primary" onClick={()=>void confirm(i)}>Confirm action</button>:i.status==='queued'?<span>{i.external_id}</span>:<button className="secondary" onClick={()=>void sync(i)}>Queue for adapter</button>}</div></article>)}</div></section>}
