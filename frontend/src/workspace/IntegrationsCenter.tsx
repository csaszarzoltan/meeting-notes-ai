const APPS=[['Microsoft Planner','Send confirmed actions with owners and deadlines.'],['Jira','Create and track work from approved meeting actions.'],['Asana','Sync tasks and completion status.'],['Linear','Route product commitments to the right team.'],['Salesforce','Attach recaps and next steps to opportunities.'],['Slack','Publish approved summaries to the right channel.']];
/** Integration catalog focused on executing approved meeting outcomes. */
export function IntegrationsCenter(){return <section><div className="page-heading"><div><span className="eyebrow">Execution connections</span><h2>Integrations</h2><p>Move approved outcomes into the tools your team already uses.</p></div><button className="secondary">Webhook settings</button></div><div className="integration-grid">{APPS.map(([name,text],index)=><article className="integration-card" key={name}><span className={`integration-logo logo-${index}`}>{name[0]}</span><div><h3>{name}</h3><p>{text}</p></div><button className={index===0?'secondary connected':'primary compact'}>{index===0?'✓ Connected':'Connect'}</button></article>)}</div></section>}
