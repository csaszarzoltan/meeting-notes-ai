import { useState } from 'react';
/** Workspace defaults for privacy, vocabulary, templates, and retention. */
export function WorkspaceSettings(){const [saved,setSaved]=useState(false);return <section><div className="page-heading"><div><span className="eyebrow">Governed defaults</span><h2>Workspace settings</h2><p>Make the safe, repeatable choice automatic for every meeting.</p></div><button className="primary" onClick={()=>setSaved(true)}>Save changes</button></div>{saved&&<p className="success-banner" role="status">Workspace defaults saved.</p>}<div className="settings-layout"><nav className="settings-nav">{['General','Privacy policy','Vocabulary','Templates','Retention','Notifications','API & webhooks'].map((name,index)=><button className={index===1?'active':''} key={name}>{name}</button>)}</nav><div className="settings-form"><h3>Privacy policy</h3><p>These defaults are shown before capture and recorded with every approved version.</p><label>Processing region<select><option>Zürich / EU</option><option>United States</option></select></label><label>Default sharing<select><option>Private until approved</option><option>Workspace after approval</option></select></label><label>Default retention<select><option>Workspace default · 3 years</option><option>1 year</option><option>6 years</option><option>7 years</option></select></label><label className="toggle-row"><span><strong>Require approval before sharing</strong><small>Always required for Healthcare and Legal meetings.</small></span><input type="checkbox" defaultChecked /></label><label className="toggle-row"><span><strong>Detect sensitive information</strong><small>Flag potential PHI and personal data for review.</small></span><input type="checkbox" defaultChecked /></label></div></div></section>}
