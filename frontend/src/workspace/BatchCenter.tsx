/** Durable batch queue with per-file progress and recovery actions. */
export function BatchCenter(){return <section><div className="page-heading"><div><span className="eyebrow">Background processing</span><h2>Batch queue</h2><p>Process collections without babysitting every recording.</p></div><button className="primary">Upload batch</button></div><div className="batch-list">{[['Customer research sprint','7 of 10 complete','70%'],['Leadership interviews','4 of 4 complete','100%'],['Legal intake · August','2 failed · 6 complete','75%']].map(([name,status,width],index)=><article className="batch-card" key={name}><span className="batch-icon">▦</span><div><strong>{name}</strong><small>{status}</small><progress max="100" value={parseInt(width)} /></div><span className={`status-pill ${index===2?'status-needs_review':'status-approved'}`}>{index===2?'Needs attention':index===1?'Complete':'Processing'}</span><button className="secondary">{index===2?'Retry failed':'View files'}</button></article>)}</div></section>}
