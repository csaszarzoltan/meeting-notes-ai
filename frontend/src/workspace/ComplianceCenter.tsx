const CONTROLS = [{ level: 'critical', title: 'PHI review incomplete', text: '3 healthcare meetings contain unresolved findings.', action: 'Review findings', checked: '4 minutes ago' }, { level: 'warning', title: 'Evidence package is stale', text: 'Quarterly access evidence was last exported 96 days ago.', action: 'Refresh evidence', checked: '2 hours ago' }, { level: 'unknown', title: 'Backup restore evidence unavailable', text: 'The latest restore exercise has no attached result.', action: 'Assign owner', checked: 'Unknown' }, { level: 'pass', title: 'Storage encryption active', text: 'All protected meeting artifacts use AES-256-GCM.', action: 'View evidence', checked: '1 minute ago' }];
/** Issue-first compliance center with evidence freshness and remediation. */
export function ComplianceCenter() { return <section aria-labelledby="compliance-title"><div className="page-heading"><div><span className="eyebrow">Evidence-backed posture</span><h2 id="compliance-title">Compliance Center</h2><p>Fix what matters, verify every claim, and export audit-ready evidence.</p></div><button className="primary">Export evidence package</button></div><div className="compliance-summary"><article className="critical"><strong>2</strong><span>Critical issues</span></article><article className="warning"><strong>4</strong><span>Required actions</span></article><article className="unknown"><strong>1</strong><span>Unknown or stale evidence</span></article><article className="pass"><strong>27</strong><span>Passing controls</span></article></div><div className="compliance-layout"><div className="control-list">{CONTROLS.map((control) => <article className={`control-card ${control.level}`} key={control.title}><span className="control-icon">{control.level === 'pass' ? '✓' : control.level === 'critical' ? '!' : '?'}</span><div><span className="status-label">{control.level}</span><h3>{control.title}</h3><p>{control.text}</p><small>Last checked: {control.checked}</small></div><button className={control.level === 'critical' ? 'primary' : 'secondary'}>{control.action}</button></article>)}</div><aside className="audit-panel"><span className="eyebrow">Traceability</span><h3>Audit history</h3>{['PHI finding assigned to Maya', 'Retention policy verified', 'Encryption evidence refreshed', 'BAA agreement exported'].map((event, index) => <div className="audit-event" key={event}><span></span><div><strong>{event}</strong><small>{index + 1}h ago · System</small></div></div>)}<button className="secondary full">Open audit explorer</button></aside></div></section>; }
