import type { WorkspaceView } from './types';
const COPY: Record<Exclude<WorkspaceView, 'home' | 'meetings' | 'record'>, [string,string,string]> = { batches:['Batch queue','Process collections without babysitting each file.','Upload a batch'], actions:['Action center','Turn commitments into completed, synchronized work.','Connect task system'], team:['Team workspace','Invite reviewers and control roles with confidence.','Invite teammate'], sharing:['Sharing center','Preview, protect, and revoke every shared meeting.','Create sharing profile'], compliance:['Compliance center','Evidence-backed controls, issues, and remediation.','Review critical controls'], integrations:['Integrations','Move approved outcomes into the tools your team uses.','Browse integrations'], settings:['Workspace settings','Define defaults for privacy, vocabulary, and retention.','Configure workspace'] };
/** Polished transitional view for existing backend capabilities awaiting full interaction wiring. */
export function PlaceholderView({ view }: { view: Exclude<WorkspaceView, 'home' | 'meetings' | 'record'> }) { const [title, description, action] = COPY[view]; return <section><div className="page-heading"><div><span className="eyebrow">Workspace</span><h2>{title}</h2><p>{description}</p></div><button className="primary">{action}</button></div><div className="feature-grid">{['Clear ownership','Safe by default','Built for review'].map((item,index)=><article className="feature-card" key={item}><span>{['↗','◈','✓'][index]}</span><h3>{item}</h3><p>{['Keep status, deadlines, and responsible people visible.','Sensitive actions use explicit permissions and auditable defaults.','Every outcome stays linked to source and human approval.'][index]}</p></article>)}</div><div className="empty-state elevated"><span className="empty-icon">✦</span><h3>Your {title.toLowerCase()} is ready to configure</h3><p>Start with the primary action above. Helpful guidance will appear here.</p></div></section>; }
