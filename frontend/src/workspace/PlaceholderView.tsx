/** Team-management surface retained while the existing team API is integrated. */
export function PlaceholderView() {
  return <section><div className="page-heading"><div><span className="eyebrow">Workspace</span><h2>Team workspace</h2><p>Invite reviewers and control roles with confidence.</p></div><button className="primary">Invite teammate</button></div><div className="feature-grid">{['Clear ownership', 'Least privilege', 'Review handoffs'].map((item, index) => <article className="feature-card" key={item}><span>{['↗', '◈', '✓'][index]}</span><h3>{item}</h3><p>{['Keep owners and reviewers visible.', 'Admin, member, and viewer roles stay explicit.', 'Move sensitive notes through a clear approval queue.'][index]}</p></article>)}</div><div className="empty-state elevated"><span className="empty-icon">♙</span><h3>Build your review team</h3><p>Invite a teammate to collaborate on verified meeting outcomes.</p></div></section>;
}
